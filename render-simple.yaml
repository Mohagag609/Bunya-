services:
  - type: web
    name: real-estate-manager
    env: static
    buildCommand: echo "Static site - no build needed"
    staticPublishPath: .
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
    branch: main
    plan: free